<!DOCTYPE html>
<html>
  <head>
    <title>HIKESanJuanIslands</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="topnav">
  <ul>
  <% if logged_in? %>
  <li><%= link_to('Log out', '/logout', method: :delete) %></li>
  <li><%= link_to 'Islands', '/islands' %></li>
  <li><%= link_to "Profile", user_path(current_user.id) %></li>
  <% else %>
  <li><%= link_to('Log in with Facebook!', '/auth/facebook', method: :post) %></li>
  <li><%= link_to 'Log in with Password', '/login' %></li>
  <% end %>
  </ul>
  </div>

    <%= yield %>


  </body>
</html>
