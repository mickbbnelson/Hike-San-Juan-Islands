<!DOCTYPE html>
<html>
  <head>
  <style>
  body {background-image: url('https://c.na84.content.force.com/servlet/servlet.ImageServer?id=01550000001kkOb&oid=00D50000000JMrK&lastMod=1461352051000');}
  </style>
    <title>HIKESanJuanIslands</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="topnav">
  <ul>
  <% if logged_in? %>
  <li><%= link_to('Log out', '/logout', method: :delete) %></li>
  <li><%= link_to 'Islands', '/islands', class: 'link-color' %></li>
  <li><%= link_to "Profile", user_path(current_user.id) %></li>
  <% else %>
  <li><%= link_to('Log in with Facebook!', '/auth/facebook', method: :post) %></li>
  <li><%= link_to 'Log in with Password', '/login' %></li>
  <% end %>
  </ul>
  </div>
  <div class="wrapper">
  <% flash.each do |type, msg| %>
    <div>
      <%= msg %>
    </div>
  <% end %>
  
    <%= yield %>

  </div>
  </body>
</html>
